{
  "name": "subhdin",
  "private": true,
  "type": "module",
  "version": "1.0.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build:api",
    "dev:api": "npm run build -w packages/shared && npm run dev -w apps/api",
    "build:api": "npm run build -w packages/shared && npm run build -w apps/api",
    "start:api": "npm run start -w apps/api",
    "lint:api": "npm run build -w packages/shared && npm run lint -w apps/api",
    "test:api": "npm run build -w packages/shared && npm run test -w apps/api"
  }
}
